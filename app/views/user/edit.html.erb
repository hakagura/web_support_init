<div class="page-header">
	<h1>Usuário / Editando</h1>
</div>
<%= form_for @user, :url => user_path(@user) do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>

<%end%>
<hr />
<%= link_to 'Exibir', @user, :class => 'btn' %>
<%= link_to 'Voltar', user_index_path, :class => 'btn' %>
