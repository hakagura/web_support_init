<div class="page-header">
	<h1>Usuário / Novo</h1>
</div>
<%= form_for @user, :url => user_index_path do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
<%end%>
<hr />
<%= link_to 'Voltar', user_index_path, :class => "btn" %>
